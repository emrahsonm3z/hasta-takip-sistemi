# Read-only patient data source (one-time seed; see CLAUDE.md §10).
# Copy this file to .env and adjust as needed.
VITE_API_URL=https://v0-json-api-three.vercel.app/api/data

# Optional: Sentry error tracking (production only; errors-only free tier).
# The DSN value is owner-managed — set it in the Vercel Production env.
# VITE_SENTRY_DSN=
